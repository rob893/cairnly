# Shared defaults for all modes. Non-secret config only — every VITE_* value is
# bundled into the browser, so never put secrets here. Per-environment overrides
# live in .env.development / .env.production; personal overrides go in .env.local.
VITE_SITENAME=Cairnly
