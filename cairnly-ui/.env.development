# Local development config (npm run dev). Non-secret — bundled into the browser.
# Points at the locally running API. Override personally in .env.local if needed.
VITE_API_BASE_URL=https://localhost:7234
